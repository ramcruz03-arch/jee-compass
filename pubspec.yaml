name: jee_compass
description: Learning engine for JEE Compass — mastery scoring, spaced repetition, mistake repair, and adaptive question selection.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
